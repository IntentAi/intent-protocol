name: PR Check

on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check branch target
        if: github.base_ref == 'main'
        run: |
          echo "::error::PRs should target a phase branch (p2, p3) or dev, not main."
          exit 1

      - name: Check PR title format
        env:
          TITLE: ${{ github.event.pull_request.title }}
        run: |
          if ! echo "$TITLE" | grep -qE '^docs(\(.+\))?: .+'; then
            echo "::error::PR title should use docs type: docs(scope): description"
            echo "Examples: docs(gateway): add reconnection spec, docs(voice): define codec negotiation"
            exit 1
          fi

      - name: Check issue reference
        env:
          BODY: ${{ github.event.pull_request.body }}
        run: |
          if ! echo "$BODY" | grep -qiE '(close[sd]?|fix(e[sd])?|resolve[sd]?) #[0-9]+'; then
            echo "::warning::PR body should reference an issue (e.g., Closes #8)"
          fi
